# Included at the end of every generated proxy server block.
# The HTTP-level map restricts writes to the exact Auth health host and path.

access_log /data/logs/hyper-auth-supabase-timing_access.log
  hyper_auth_supabase_timing
  if=$hyper_auth_supabase_health_timing_enabled;
