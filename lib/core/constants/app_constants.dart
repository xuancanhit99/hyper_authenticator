// lib/core/constants/app_constants.dart
const String cAppName = 'Hyper Authenticator';

const double cDefaultPadding = 16.0;
const Duration cAnimationDuration = Duration(milliseconds: 300);

const cDefaultSize = 30.0;